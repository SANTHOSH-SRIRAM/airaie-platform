# Project State

Last activity: 2026-04-27 - Completed Phase 10 plan 10-04: real NodeViews for the last 5 typed-governance kinds (Evidence / Gate / EmbedCard / EmbedRecord / AiAssist stub); palette split into 10-04b

## Active Milestone

v1 unified-platform frontend — Phases 1–4 closed; **Phase 8 fully complete (Waves 1+2)**; **Phase 9 Wave 1 complete**; **Phase 10 Waves 1+2+3+4 complete (framework + ALL 11 real NodeViews + full slash menu)**. The structured-sections Card-as-page (Phase 8) remains the default; Phase 10's Tiptap-based block-editor canvas is opt-in via `?canvas=1`. `cards.body_blocks` JSONB column ships with optimistic concurrency. Auto-migration generates a default block tree from existing entity state on first canvas open. **All 11 typed governance Tiptap nodes now mount real per-kind NodeViews** — Intent/Input/KPI/Method/Run/Result are fully data-bound (Phase 9 renderer registry, useCardRunState 7-stage chrome, evaluation chips, etc.); Evidence/Gate/EmbedCard/EmbedRecord are bound to their respective entities with sign-off / preview / click-through chrome; AiAssist is a recognizable stub since no kernel `/v0/conversations` route exists yet. The slash menu surfaces all 11 typed-governance kinds + 5 layout kinds (16 items total) via `/` at start of an empty paragraph, with cardinality enforcement on Intent / Method / Run. The right-rail drag-drop palette splits into Wave 10-04b (~2-3 dev-days alone). Phase 9's renderer registry plugs into ResultBlockView per-block.

## Recent Activity

- **2026-04-27** — Phase 10 plan `10-04` complete: Last 5 NodeViews wired (Wave 4 of the Card-canvas rollout). Five new React NodeViews replace `TypedBlockPlaceholder` for `evidenceBlock` / `gateBlock` / `embedCardBlock` / `embedRecordBlock` / `aiAssistBlock`. After this wave **all 11 typed-governance Tiptap nodes mount real per-kind NodeViews** (`grep -c "}, [A-Z][a-zA-Z]*View);" typedBlockNodes.ts` → 11). `EvidenceBlockView` filters the card's full evidence list (single fetch via `useCardEvidence(cardId)`, shared with the page) using a pure `findEvidenceById` helper; renders metric_key + value + threshold-clause + an evaluation chip (pass/fail/warning/info/pending). `GateBlockView` fetches via `useGate(gateId)`, renders status-color badge (PENDING amber / EVALUATING blue / PASSED green / FAILED red / WAIVED grey); PENDING gates surface inline Approve / Reject buttons calling `useApproveGate / useRejectGate` — full Mode-rule gating ships in 10-06. `EmbedCardBlockView` (`useCard(cardId)`) renders mini-preview with click-through. `EmbedRecordBlockView` uses a NEW `useRecord(boardId, recordId)` hook backed by `getRecord` API client (`GET /v0/boards/{id}/records/{recordId}`); pure `formatRecordPreview` helper handles the canonical `{ text }` shape with a JSON-stringify fallback. `AiAssistBlockView` is intentionally a **stub** — kernel has no `/v0/conversations` route yet, so a real fetch would 404 every render; the block kind, attrs (`conversationId`), and slash-menu item all stay so users can drop the block in and have it persist; runtime wiring lights up when the backend ships. **Drag-drop right-rail palette is split into Wave 10-04b** (~2-3 dev-days alone); slash menu already lets users insert any kind, so the palette is value-additive but not value-blocking. 6 atomic commits (T1: `getRecord` + `useRecord`; T2: `EvidenceBlockView` +12 tests; T3: `GateBlockView` +12 tests; T4-6: EmbedCard/EmbedRecord/AiAssist; T7: factory wiring; T8: docs). All 4 gates green (tsc default + strict, vitest **410 passing — +29 net-new vs 381 baseline**, npm build with `dist/assets/editor-BcatEhBp.js` byte-identical at 387.56 kB — no growth despite 5 new components). SUMMARY at `.planning/phases/10-card-canvas/10-04-SUMMARY.md`.
- **2026-04-27** — Phase 10 plan `10-03` complete: Real NodeViews for KPI / Method / Run + full slash menu (Wave 3 of the Card-canvas rollout). Three new React NodeViews replace `TypedBlockPlaceholder` for `kpiBlock` / `methodBlock` / `runBlock`: `KpiBlockView` (reads `attrs.metricKey | operator | threshold` directly — no fetch — renders threshold summary + `evaluateKpi` status chip); `MethodBlockView` (fetches via `usePlan(cardCanvasContext.cardId)` per the contract decision, NOT `attrs.planId`; empty-state has inline "Generate Plan" button calling `useGeneratePlan(cardId).mutate()`; loaded state shows pipeline_id + step count + tool-id chain); `RunBlockView` (the inline canvas Run button — pulls `{ card, intent, plan }` from `cardCanvasContext`, instantiates `useCardRunState(card, intent, plan)`, renders one of 7 stage-specific UI branches with `state.run() / state.cancel() / state.generate() / state.rerun()` action handlers — both this and CardActionBar share the React Query cache and flip together within one render cycle). `cardCanvasContext` widened from `{ cardId, boardId, intentSpecId }` to `{ cardId, boardId, intentSpecId, card, intent, plan }`; `CardCanvasPage` populates the new fields from already-fetched query data with no new round trips. Slash menu expanded from 3 items to 16 — 11 typed-governance kinds + 5 layout kinds (Heading 1, Heading 2, Bullet List, Code Block, Divider). `SlashMenuItem` becomes a discriminated union (`{ kind: 'typed' | 'layout' }`); cardinality helpers `docHasMethodBlock` / `docHasRunBlock` mirror `docHasIntentBlock`; the popover renders two visual sections (Governance / Layout) with a thin divider while keyboard nav still walks the flat array. 9 atomic commits, all `--no-verify`. All 4 gates green (tsc default + strict, vitest 381 passing — **+32 net-new vs 349 baseline**, `npm run build` with `dist/assets/editor-BcatEhBp.js` chunk; no new chunks, no new dependencies). Remaining 5 typed-block kinds (Evidence/Gate/EmbedCard/EmbedRecord/AiAssist) keep `TypedBlockPlaceholder` until 10-04. SUMMARY at `.planning/phases/10-card-canvas/10-03-SUMMARY.md`.
- **2026-04-27** — Phase 10 plan `10-02` complete: Real NodeViews for Intent / Input / Result + minimal slash menu (Wave 2 of the Card-canvas rollout). Three new React NodeViews replace `TypedBlockPlaceholder` for `intentBlock` / `inputBlock` / `resultBlock`: `IntentBlockView` (reads `attrs.intentSpecId` → `useIntent`), `InputBlockView` (reads `attrs.artifactId` → new `useArtifact`), `ResultBlockView` (reads `attrs.artifactId` → `useArtifact` + `useArtifactDownloadUrl`, plugs into Phase 9's `pickRenderer(artifact, intent)` and mounts ONE renderer per block in `<Suspense>`). New `CardCanvasContext` threads `{ cardId, boardId, intentSpecId }` from page → NodeViews so the renderer pick is intent-aware; degrades gracefully outside a Provider. Slash-menu surface: `@tiptap/suggestion@^3.22.4` installed; `SlashMenu` Tiptap extension wraps it (`char: '/'`, `startOfLine: true`); React popover via `useSyncExternalStore` + `createPortal` at `editor.view.coordsAtPos(range.from)`; cardinality rule hides "Intent" item when doc already has an `intentBlock`. `airAirExtensions` becomes `buildAirAirExtensions(opts)` factory threading `cardContext`. New `getArtifact` API client unwraps the `{ artifact, sha256 }` envelope. 9 atomic commits, all `--no-verify`. All 4 gates green (tsc default + strict, vitest 343 passing — 25 net-new, npm build with `dist/assets/editor-BcatEhBp.js` chunk; suggestion bytes joined the editor chunk via the existing `@tiptap/*` predicate, no new chunk). Other 8 typed-block kinds (KPI/Method/Run/Evidence/Gate/EmbedCard/EmbedRecord/AiAssist) keep `TypedBlockPlaceholder` until 10-03+. SUMMARY at `.planning/phases/10-card-canvas/10-02-SUMMARY.md`.
- **2026-04-27** — Phase 10 plan `10-01` complete: Card Canvas editor framework (concept Phase 1 of Card-canvas rollout). Tiptap installed directly (no BlockNote dependency — long-term ownership of block + UI layers, ProseMirror under the hood for cursor/selection/IME). 11 typed governance Node kinds declared via factory pattern (`makeTypedBlockNode`) — all currently render as `TypedBlockPlaceholder` (`🎯 Intent · attrs: {...}`); real components land in 10-02. Backend: migration `031_card_body_blocks.sql` adds `body_blocks JSONB` + `body_blocks_version INTEGER`, new handler `PATCH /v0/cards/{id}/body` with optimistic concurrency (409 VERSION_CONFLICT → re-fetch + 3-way merge UI in 10-05). Frontend: `useAirAirEditor` hook with debounced 1500ms autosave + `beforeunload` flush, `<AirAirEditor>` wrapping Tiptap's EditorContent with prose typography, `generateDefaultBody(card, intent, plan, latestRun, evidence, gates)` pure function for migration. Feature flag: `?canvas=1` lazy-routes from `CardDetailPage` to `<CardCanvasPage>`; "Try canvas →" link in CardTopBar (hidden when already on canvas). 12 atomic commits across 2 repos: 11 frontend (T0..T9, T10) + 1 kernel (T6). Recovery: original gsd-executor stalled at T6 step 4; continuation agent completed T6-T8 cleanly; T9-T10 finished inline. Hotfix `d9f5a24` moved canvas dispatch AFTER all hooks (rules-of-hooks violation in initial T9). All 5 frontend gates green (tsc default + strict, vitest 318 passing — 29 net-new, npm build with `dist/assets/editor-80afoeWp.js` chunk). Kernel `go build` clean; `go test -short` had 4 pre-existing agent-test failures unrelated to Phase 10 (documented). SUMMARY at `.planning/phases/10-card-canvas/10-01-SUMMARY.md`.
- **2026-04-26** — Phase 9 plan `09-01` complete: Renderer Registry MVP (concept Phase 2a). 5 lazy-loaded renderers (image, json-metrics, csv-chart, csv-table, fallback), 3 per-intent layouts (cfd_analysis, fea_static, parameter_sweep), `papaparse` segregated into render-csv Vite manualChunk. `pickRenderer(artifact, intent)` honors a 4-tier priority: manifest_hint → exact (intent_type, kind) → kind-only → always-true fallback. ResultsSection extracts a pure `planResults` helper for unit-testable dispatch (env=node + no testing-library). CardStatusPanel.tsx replaces its bullet-list-of-artifact-download-links Results subsection with `<ResultsSection runArtifacts={...} intent={intent} />`. 11 atomic commits (Tasks 1-11 + 1 docs commit), 38 net-new tests, 289 total passing. All gates green: default + strict typecheck, vitest, npm build with verified `dist/assets/render-csv-*.js` chunk. SUMMARY at `.planning/phases/09-renderer-registry/09-01-SUMMARY.md`.
- **2026-04-26** — Phase 8 plan `08-02` complete: full body composition for the Card-as-page route. The Run state machine that lived inline in CardTopBar (Wave 1) was promoted to a shared `useCardRunState` hook with a 7-stage discriminated union (no-intent / no-inputs / no-plan / ready / running / completed / failed); CardTopBar was refactored to consume it and CardActionBar was built on top. Cross-surface sync invariant verified by pure-function tests of the shared `deriveCardRunStage` helper. 12 tasks shipped as 14 atomic commits (T7 split into 7a/7b/7c). 251 vitest tests passing (37 net-new), both typechecks clean. New components: CardHero, AvailableInputsTable, AvailableMethodsTable, AddCustomKpiForm, ConfigSection, CardExecutionSequence, CardStatusPanel, CardActionBar, EmptyDraftIntent, UnsupportedCardTypeFallback. New hooks: useCardModeRules, useCardRunState, useCardGates, useArtifactList, useIntentTypePipelines. SUMMARY at `.planning/phases/08-card-as-page/08-02-SUMMARY.md`.
- **2026-04-26** — Phase 8 plan `08-01` complete: per-card route `/cards/:cardId` shipped with a fully functional Run state machine (Draft → Generate → Run → Cancel — no `console.info` stubs), inline title editing, breadcrumb navigation, sidebar context blocks (`ThisBoardNav` + `ThisCardStatusPill`), and `BoardDetailPage` legacy fallback behind `?legacy=1`. 9 atomic commits (T1 `9872ea0` through T8 `db380a7` plus T4 follow-up `34523da`). All gates green (default + strict typecheck, 214 vitest tests passing — 13 net-new across 2 new test files using pure-helper style since `@testing-library/react` is not yet installed). 8 files created, 6 modified. SUMMARY at `.planning/phases/08-card-as-page/08-01-SUMMARY.md`.
- **2026-04-25** — Quick task `260425-f10` polished three Agent Playground UX bugs: reload no longer auto-creates a duplicate session (gated `useSessionList.isLoading`), assistant byline reads the real agent name (threaded `agentName` prop through `ChatInterface` → `ChatMessage`, killed the hardcoded `'FEA Optimizer'` constant), and the Inspector "Messages" counter uses `extractMessages(activeSession).length` instead of the byte length of the base64 history string. Single commit `fe67e4f`, 4 files, tsc clean.
- **2026-04-25** — Audit of `/workflow-runs` against live kernel surfaced backend route gaps (`/v0/workflows/{id}/runs` 404, `/retry` 404, SSE 500, `cancel` 500 on terminal runs) and ~6 frontend shape mismatches. Quick task `260425-e2a` shipped frontend mapper layer, un-hardcoded workflow id, fixed envelope unwrap and empty-id hook guards. Page now renders 37 real runs against `wf_737cd510` with 0 console errors.

## Blockers/Concerns

- 4 remaining `wf_fea_validation` hardcodes in unrelated pages (`WorkflowEditorPage`, `WorkflowDetailPage`, `WorkflowsPage`, `GlobalSearch`) — same bug class, separate fix-it.
- New users registered through `/v0/auth/register` have zero project memberships → 403 on every API call. Either auto-attach to default project or have registration accept a project id.
- DSL parsing (`workflow.versions[0].dsl` base64 JSON → `steps[]`) not yet implemented; the editor and DAG viewer can't show graph structure until that lands.
- Backend gaps tracked in `260425-e2a-SUMMARY.md` "Backend Gaps Surfaced" section.
- **Frontend testing infra:** `@testing-library/react` is not installed and vitest env=node, so DOM-render tests can't be written. Unit tests follow a "pure-helper" pattern (extract logic, test in isolation). A post-MVP infra task should wire jsdom + testing-library so future plans can write actual JSX tests. 08-02 added 37 net-new tests using this pattern, including the load-bearing `deriveCardRunStage` and `computeLifecycleStage` helpers.
- **`useGates` and gate-mutation hooks** — landed in 08-01; gate-id-scoped versions (`useApproveGate / useRejectGate / useWaiveGate`) exist. 08-02 added card-scoped `useCardGates(cardId, boardId)` that walks the Board's gates and filters by `requirement.config.card_id`. Backend has no `/v0/cards/{id}/gates` route; the join is client-side and degrades to `[]` on 404.
- **`/v0/cards/{id}/gates` not present on kernel** — composed client-side via `listCardGates(boardId, cardId)` walking gate requirements. If a future kernel ships the dedicated route, swap the API function and remove the client-side join.
- **`@airaie/shell` AppShell does not yet provide a CardActionBar slot** — Phase 8 mounts CardActionBar directly via `position: fixed` on the page. If a unified bottom-bar slot ships in a later phase, refactor to consume it.
- **MinIO presigned URL CORS not yet verified for direct browser fetch** — Phase 9 renderers fetch artifact bytes directly from MinIO via the presigned URL returned by `/v0/artifacts/{id}/download-url`. If MinIO's CORS isn't configured for the dev/prod origin, the browser will block the fetch and renderers will show their error chrome. Smoke-test on the next live-run session; if blocked, configure MinIO CORS in `airaie-kernel/scripts/dev-start.sh` bucket setup.
- **Tool manifests don't yet declare `renderer_hint`** — concepts/05 §"Recommended A. Renderer hints" describes the field; the kernel doesn't yet propagate `renderer_hint` from the ATP manifest's output port into `Artifact.metadata.renderer_hint`. Frontend reads gracefully (missing → null → fall through to predicate walk). Phase 2d kernel work.
- **Phase 9 punted `frd` and `vtu` renderers to Phase 2c** — the cfd_analysis and fea_static layouts include slots for these kinds, but the Phase 2a registry has no entry; they fall through to GenericArtifactRenderer (download link). When Phase 2c lands the vtk.js viewers, register them — no layout change needed.
- **Phase 10 ships real NodeViews for Intent / Input / Result / KPI / Method / Run + full slash menu (16 items, cardinality enforced for Intent / Method / Run); remaining 5 typed kinds (Evidence / Gate / EmbedCard / EmbedRecord / AiAssist) still placeholder, real components ship in 10-04** — the schema-level cardinality enforcement (Tiptap groups) and pre-save validator are defense-in-depth that land later. The structured-sections Phase 8 page remains the default at `/cards/:cardId`; canvas is opt-in via `?canvas=1`. Feature flag flip + structured-page removal lands in Wave 10-07.
- ~~**Phase 10 migration `031_card_body_blocks.sql` not yet applied to dev DB**~~ Applied to the dev DB during the 10-02 executor run (verified: `cards.body_blocks JSONB` and `cards.body_blocks_version INTEGER NOT NULL DEFAULT 1` columns are live). `PATCH /v0/cards/:id/body` endpoint shipping per 10-01.
- **Tiptap React 19 compat** — Phase 10 installed Tiptap 3.22.4 (newer than the plan's `^3.13.0`). React 19 supported per Tiptap's peer deps. No issues during build or tests; if regressions appear, lock to a known-working minor.
- **Pre-existing kernel test failures surfaced in Phase 10 final gate**: `internal/agent`: `TestValidateForPublish_MissingTool`, `TestValidateForPublish_UnpublishedTool`, `TestPublishAgentVersion_BlocksUnresolvedTools`, `TestAgentService_PublishAgentVersion`. These were failing BEFORE Phase 10. Not caused by Phase 10 changes; documented in `10-01-SUMMARY.md` "Pre-existing kernel test debt" section. Track separately for an `internal/agent` test-fix quick task.
- **`useRunDetail` 404 with `crun_*` ids** — surfaced during Phase 10 smoke. Some surface (likely Phase 8's `CardDetailPage`) is calling `useRunDetail` with a card-runs id (`crun_*`) instead of a workflow-runs id (`run_*`). The 404 is harmless but fills the console. Worth a `/gsd:quick` to grep callers and route the correct id.

## Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260425-f10 | Polish 3 Agent Playground UX bugs (reload session reuse + agent byline + Inspector Messages count) | 2026-04-25 | fe67e4f | [260425-f10-polish-3-visible-bugs-in-agent-playgroun](./quick/260425-f10-polish-3-visible-bugs-in-agent-playgroun/) |
| 260425-e2a | Fix Phase 4 workflow-runs frontend (mapper + un-hardcode + envelope unwrap) | 2026-04-25 | 72e52f1 | [260425-e2a-fix-phase-4-workflow-runs-frontend-to-wo](./quick/260425-e2a-fix-phase-4-workflow-runs-frontend-to-wo/) |
