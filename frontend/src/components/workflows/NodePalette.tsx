/**
 * Re-export from the new palette directory.
 * The canonical implementation lives in ./palette/NodePalette.tsx.
 * This file preserves backward compatibility with existing imports.
 */
export { default } from './palette/NodePalette';
